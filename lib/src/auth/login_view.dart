import 'package:flutter/material.dart';
import 'package:libsys/src/forms/login_form.dart';

class LoginView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: LoginForm(),
    );
  }
}
